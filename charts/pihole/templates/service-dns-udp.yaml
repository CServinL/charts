apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-dns-udp
spec:
  type: NodePort
  externalTrafficPolicy: Local
  selector:
    app: {{ .Release.Name }}
  ports:
    - name: dns-udp
      port: 53
      targetPort: dns-udp
      protocol: UDP
